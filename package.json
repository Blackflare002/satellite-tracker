{
	"name": "root",
	"version": "0.1.0",
	"private": true,
	"proxy": "http://localhost:8000/",
	"devDependencies": {
		"concurrently": "^7.6.0",
		"nodemon": "^2.0.20",
		"npm-run-all": "^4.1.5",
		"react-scripts": "^5.0.1"
	},
	"scripts": {
		"start": "concurrently \"cd client && react-scripts start\" \"cd server && nodemon server/server.js\"",
		"build": "concurrently \"cd client && yarn install\" \"cd server && yarn install\""
	}
}
